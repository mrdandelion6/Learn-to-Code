#include <stdlib.h>
#include <stdio.h>

int main() {

    print("size of char pointer: %d\n", sizeof(char*));
    print("size of int pointer: %d\n", sizeof(int*));
    print("size of float pointer: %d\n", sizeof(float*));
    print("size of double pointer: %d\n", sizeof(double*));
}